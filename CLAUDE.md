# グローバル設定 (User Memory)

> このファイルは `~/.claude/CLAUDE.md` に配置してください。
> すべてのプロジェクトに共通で適用されます。

## 基本ルール

- 日本語で応答すること
- コードのコメントは日本語で書くこと
- コミットメッセージは英語で簡潔に書くこと（例: `fix: resolve login redirect issue`）
- 変数名・関数名は英語で、キャメルケースを使用すること

## 禁止事項

- `rm -rf` や `rm -r` などの再帰的・強制削除コマンドは絶対に実行しないこと
- ファイル削除が必要な場合は、対象ファイルを明示して個別に削除すること（必ず事前に確認を求める）
- ディレクトリ削除が必要な場合は、中身を一覧表示してユーザーに確認を取ってから実行すること
- `find ... -delete` などの一括削除系コマンドも使用しないこと
- `.env` ファイルや API キー、秘密鍵の内容を会話に含めないこと
- `sudo` コマンドは使用しないこと
- `git push --force` は使用しないこと

## セキュリティルール

### 読み取り禁止ファイル
- `.env*`（環境変数ファイル全般）
- `**/*.key`、`**/*.pem`（秘密鍵）
- `**/credentials.json`（認証情報）
- `**/google-services.json`（Firebase設定）

### 会話に含めない情報
- API キー、シークレットキー
- パスワード、トークン
- 個人情報（メールアドレス、電話番号など）

## 作業スタイル

- 大きな変更を行う前に、まず計画を提示して確認を求めること
- ファイルを編集する前に、現在の内容を確認すること
- エラーが発生した場合は、原因を分析してから修正すること（闇雲に変更しない）
- テストがある場合は、変更後に必ずテストを実行すること
